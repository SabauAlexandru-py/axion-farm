# Axion Farm

Instagram + Outlook mass account creation control plane.

## Run locally

```bash
npm install
npm run dev
```

Open http://localhost:3000

For real phone automation set `DEVICE_UDID` in `.env` and run Appium on your Mac.
